name: go
dependencies: []
arguments:
  org:
    required: true
    description: Git Service Organization, generally $(dirname) Go module path
  type:
    required: true
    description: Type of service this Go application is.
    type: list
    values:
      - grpc
      - http
